{
  "name": "@crm-agent/test-fixtures",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@crm-agent/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
